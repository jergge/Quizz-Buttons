#pragma once
#include "IODevice.h"
#include "LinkedList.h"

class UpdateRegister
{
    // private:
    //     static LinkedList<UpdateableObject> list;
    //     static int a;

    // public:
    //     static void Register(UpdateableObject * device)
    //     {
    //         list.Append(device);
    //     }
    //     static void UnRegister(UpdateableObject device) {
    //          //to do (may not be needed for the quizz box project as it should just be reset...)
    //     }

    //     static void UpdateAll()
    //     {
    //         for (int i = 0; i < list.Count(); i++)
    //         {
    //             list.Get(i).DataPtr()->Update();
    //         }
    //     }

};